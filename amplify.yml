version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .
    files:
      - build/**/*
      - node_modules/**/*
      - package.json
  cache:
    paths:
      - node_modules/**/*
